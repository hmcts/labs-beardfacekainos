version: '2.1'

services:
  beardfacekainos:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: hmctsprod.azurecr.io/labs/beardfacekainos
    ports:
      - $SERVER_PORT:$SERVER_PORT
